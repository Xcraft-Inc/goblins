const {generateFile, tryGenerateFile, jsify} = require('../helpers.js');
module.exports = (appId, noInstall) => {
  appId = jsify(appId);
  console.log(`✨ initializing ${appId} API #I've got what you need!
  
    `);
};
